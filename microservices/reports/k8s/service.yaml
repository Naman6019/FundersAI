apiVersion: v1
kind: Service
metadata:
  name: reports-service
spec:
  selector:
    app: reports-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8001
      nodePort: 30001
  type: NodePort
