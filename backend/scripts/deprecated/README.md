# Deprecated Scripts

These files are retained only for historical one-off migration work.

They are not used by production routes, `/api/chat`, comparison canvas logic, or GitHub Actions jobs. FundersAI does not require Screener exports for active stock data.
